/** Must stay aligned with the compact shell boundary in guided-workspace.css. */
export const COMPACT_NAVIGATION_MAX_WIDTH_PX = 1279;
export const COMPACT_NAVIGATION_MEDIA_QUERY = `(max-width: ${COMPACT_NAVIGATION_MAX_WIDTH_PX}px)`;
