/** Must stay aligned with the compact shell boundary in responsive-shell.css. */
export const COMPACT_NAVIGATION_MAX_WIDTH_PX = 760;
export const COMPACT_NAVIGATION_MEDIA_QUERY = `(max-width: ${COMPACT_NAVIGATION_MAX_WIDTH_PX}px)`;
