# Canonical local entry point. This delegates to the supported LedgerSync
# topology; production environments use reviewed infrastructure-as-code rather
# than Compose.
include:
  - path: deploy/compose/docker-compose.yml
